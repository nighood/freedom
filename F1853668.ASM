EXTRN	DISPBX:FAR
MYDATA SEGMENT
DATA  	DW      1234H,5678H,9D4CH,0D7H,0,-1,7D2AH,8A0EH,10F5H,645DH,7FFCH,7FFDH,7FFAH
N   	EQU     $-DATA
WARN	DB	0AH,0DH,"THERE EXISIT NO EVEN.$"
MYDATA ENDS

CODE    SEGMENT
        ASSUME  CS:CODE,DS:MYDATA
MAIN	PROC	FAR
	PUSH	DS
	MOV	AX,0
	PUSH	AX
	MOV	AX,MYDATA
      	MOV	DS,AX
	MOV	AX,N
	MOV	BL,2
	DIV	BL
      	MOV	CL,AL
	MOV	CH,0
	MOV	DX,0;为了防止字符串中无偶数
	MOV	BX,OFFSET DATA
      	MOV	AX,8000H;最小的带符号数
AGAIN:  	TEST	WORD PTR DS:[BX],1
	JNZ	NEXT
        	CMP	AX,[BX]
        	JG	NEXT
        	MOV	AX,[BX]
	INC	DX
NEXT:   	INC	BX
	INC	BX
	LOOP	AGAIN
	CMP	DX,0
	JNZ	DOWN
	LEA	DX,WARN
	MOV	AH,9
	INT	21H;输出无偶数错误提示
	RET
DOWN:    MOV	BX,AX
	CALL	DISPBX
	RET
MAIN	ENDP
CODE	ENDS
END	MAIN